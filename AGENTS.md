# AGENTS.md

> СТРОГОЕ ПРАВИЛО НАЧАЛА СЕССИИ: Первым действием всегда читай `_STATE_.md`.
> Если в `_STATE_.md` зафиксирован незавершённый шаг -> переходи в режим "Продолжение".
> Если состояние `done` или файл отсутствует -> переходи в режим "Новая задача".

## Артефакты и правила доступа к ним

Воркфлоу построен строго вокруг следующих артефактов. Нарушение правил доступа к ним является критической ошибкой:

- `_STATE_.md` — Состояние текущей сессии. Твоя единственная точка входа. Вся оперативная информация, шаги и контекст живут ТОЛЬКО здесь. Создание любых других файлов состояния (например, `plan-*.md` или `.current-plan.json`) СТРОГО ЗАПРЕЩЕНО.
- `_TODO_/` — Инбокс проекта. Содержит файлы с замечаниями или задачами. Ты имеешь право читать этот каталог (исключая подкаталог `done/`) только на Шаге 2. Твоя цель — перенести задачи отсюда в `ROADMAP.md` и сразу переместить отработанные файлы в `_TODO_/done/`.
- `ROADMAP.md` — Единый долгосрочный технический план проекта. Изменяется строго по согласованию с `_STATE_.md`. Порядок этапов менять запрещено.
- `_DRAFTS_/` — Черновики в процессе написания. ТЕБЕ ЗАПРЕЩЕНО читать этот каталог, открывать файлы из него или использовать их как контекст. Игнорируй его существование, пока пользователь сам не перенесёт файлы в `_TODO_/`. Живой воркфлоу ТЗ этот каталог не использует — проекты спецификаций согласуются в чате (см. Регламент §2).
- `docs/` — Единое хранилище технических спецификаций (ТЗ). Изменяется ТОЛЬКО по явному разрешению пользователя (Регламент §2). Каждая задача в `ROADMAP.md` обязана ссылаться на параграф/якорь документа здесь (Регламент §3).
- `examples/` — Справочные примеры кода и решений. ТЕБЕ ЗАПРЕЩЕНО мутировать, изменять или перезаписывать файлы в этом каталоге. Доступ только на чтение (`read`).


Шаг 0: Инициализация и состояние

1. Прочитай файл `_STATE_.md`.
2. Определи режим работы по следующим правилам:

   - **РЕЖИМ "ПРОДОЛЖЕНИЕ"** (Если в файле есть незавершённые шаги `[ ]` или статус не равен `done`):
     - Считай из файла: `активная задача`, `current_step`, `следующий ход` и `изменяемые файлы`.
     - Выведи пользователю текстовое подтверждение: *"Вхожу в режим Продолжения. Текущий шаг: [название]. Работаю со следующими файлами: [список]"*.
     - Переходи строго к выполнению указанного `current_step`, не пересчитывая план заново.

   - **РЕЖИМ "НОВАЯ ЗАДАЧА"** (Если файл отсутствует, пустой или его состояние `done`):
     - Создай (или полностью перезапиши) `_STATE_.md` строго по следующему шаблону:

```markdown
# Текущая микро-сессия

- **Задача из ROADMAP:** [Номер и название задачи]
- **Вайтлист файлов в работе (Изменяемые файлы):**
  - [Путь к файлу 1]
- **Критерий успеха (Definition of Done):** [Четкий технический критерий, например: тесты проходят, компиляция ок]

## Итерационный трекер
[ ] Шаг 1: [Описание микро-шага]

- **Текущий шаг (current_step):** Шаг 1
- **Следующий ход:** [Что делать прямо сейчас]
- **Счетчик безуспешных компиляций:** 0/3
- **Состояние:** in_progress
```

     - Заполни в шаблоне только базовые поля, если задача уже известна, либо оставь плейсхолдеры до завершения Шагов 1–3.


## Лёгкий путь (тривиальные правки)

Если задача — точечный фикс, затрагивающий **ровно 1 файл** и **не требующий** новой функциональности (опечатка, константа, однострочный баг, комментарий), полный цикл Шаг 0–5 можно сократить:

1. `git status --porcelain` — репозиторий должен быть чистым (иначе действует общее правило Шага 1).
2. Внеси правку.
3. `cargo check` (и `cargo test <относящийся_фильтр>`, если правка затрагивает покрытую тестом логику).
4. Коммит с понятным сообщением. `_STATE_.md`/`ROADMAP.md` трогать не обязательно.

Если в процессе выясняется, что правка требует больше 1 файла или превращается в несколько логических шагов — прекрати лёгкий путь и перейди к полному воркфлоу (Шаг 0–5).

Шаг 1: Аудит Репозитория (Безопасность)

1. Выполни команду `git status --porcelain`.
2. На основе результатов определи чистоту репозитория и режим сессии:

   - **Если репозиторий "ГРЯЗНЫЙ" (есть незакоммиченные изменения):**
     - Выполни `git diff --stat` и выведи отчет о текущих изменениях в чат.
     - **В режиме "Новая задача":** СТОП. Запрещено вносить изменения. Выведи в чат сообщение: 
       *"Репозиторий содержит незакоммиченные изменения. Пожалуйста, выберите действие: 1) Сделать WIP-коммит, 2) Выполнить git stash, 3) Отменить изменения (git reset). Ожидаю вашей команды."* Не продолжай работу без явного ответа пользователя.
     - **В режиме "Продолжение":** Сверь список фактически изменённых файлов из `git status` со списком в разделе `Вайтлист файлов в работе` в `_STATE_.md`.
       - *Если файлы СТРОГО СОВПАДАЮТ:* Продолжай работу.
       - *Если есть любые расхождения (лишние или недостающие файлы):* СРОЧНЫЙ СТОП. Выведи в чат ошибку: *"Внимание! Обнаружено расхождение между Git и файлом состояния сессии. Требуется ручное вмешательство."* Ничего не изменяй в репозитории.

   - **Если репозиторий "ЧИСТЫЙ":** Продолжай работу.

3. Выполни базовую проверку компиляции проекта командой `cargo check --quiet`.
   - **Если компиляция успешна:** Переходи к Шагу 2.
   - **Если компиляция упала с ошибкой (`Error`):** СТОП. Вносить изменения в код запрещено. Перейди в интерактивный режим диагностики и выведи в чат сообщение:
     
     *"🚨 Обнаружена ошибка компиляции проекта ДО начала работы. 
     Я сохраняю режим 'Чтение' (Read-only) и не буду менять файлы без команды. 
     
     Пожалуйста, выберите действие:
     1) [Диагностика] — Попросить меня проанализировать логи и код, чтобы найти причину поломки.
     2) [Ручной ремонт] — Вы исправите ошибку самостоятельно и введёте 'готово' для повторной проверки."*

4. **Воркфлоу Режима Диагностики** (Активируется ТОЛЬКО если пользователь выбрал вариант 1):
   - Изучи лог ошибки `cargo check`. Найди проблемный файл и строку.
   - Прочитай этот участок кода (используй строго инструмент `read`, инструмент `write/patch` ЗАПРЕЩЕН).
   - Выведи пользователю краткий и понятный технический отчет:
     - **Локализация:** Файл, строка, какая функция сломана.
     - **Причина:** Почему упало (например: не совпадает тип, пропущен аргумент, мёртвая зависимость).
     - **Готовое решение:** Покажи в чате кусок кода (дифф), который исправит проблему.
   - Заверши отчет вопросом: 
     *"Я могу применить это исправление автоматически? Ответь: 'Да' (для авто-применения) или 'Нет' (если исправишь сам)."*
   - **Если пользователь ответил 'Да':** Примени конкретный дифф, выполни `cargo check` повторно. Если компиляция стала зелёной -> приведи `_STATE_.md` к начальному виду и иди на Шаг 2. Если снова упало -> СТОП, вернись к выбору ручного ремонта.


Шаг 2: Обработка инбокса _TODO_

1. Отсканируй каталог `_TODO_/` (строго игнорируй подкаталог `done/`, каталог `_DRAFTS_/` и любые файлы в `examples/`).
2. Если в инбоксе пусто -> сразу переходи к Шагу 3.
3. Если обнаружены новые файлы задач/замечаний, обработай каждый файл строго по следующему алгоритму:

   - **Анализ и Дедупликация:** Внимательно прочитай текст новой задачи и сравни её с существующими пунктами в `ROADMAP.md`. 
     - *Правило строгого матчинга:* Пропускай задачу только в том случае, если в `ROADMAP.md` уже есть пункт, полностью покрывающий это техническое исправление. Если новая задача содержит дополнительные нюансы или другие файлы — она считается **уникальной**.
     - *Правило критического анализа примеров:* Если в задаче из `_TODO_/` есть ссылка на файлы в каталоге `examples/`, прочитай их исключительно для понимания общей концепции или алгоритма. **ВНИМАНИЕ:** Код в `examples/` может быть непроверенным черновиком или внешним фрагментом. ТЕБЕ ЗАПРЕЩЕНО слепо копировать его стиль, структуру или подходы. При реализации ты обязан полностью переработать этот алгоритм под строгие стандарты текущего проекта: использовать только безопасные Rust idioms, исключить `unwrap()`, следовать "ромб-декомпозиции" `MusicApp` и не нарушать существующую архитектуру.

   
   - **Классификация (Определение приоритета):** Присвой уникальной задаче приоритет строго по следующим критериям:
     - `🔴 Критический` — Паники, утечки памяти, гонки потоков, зависания UI, блокирующий ввод-вывод.
     - `🟡 Высокий` — Нарушение ключевой бизнес-логики, сломанные ресемплеры/декодеры, регрессия в аудио-движке.
     - `🟢 Средний / 🔵 Низкий` — Оптимизация производительности, рефакторинг кода, добавление новых настроек в UI.

   - **Безопасная вставка в ROADMAP.md:** Добавь задачу в соответствующий этап и таблицу в `ROADMAP.md`.
     - *Правило атомарного патча:* Не переписывай файл целиком с нуля. Используй инструмент точечного редактирования (`patch/diff`). Убедись, что существующие пункты, статусы и коммиты других задач НЕ задеты и форматирование таблицы не сломалось.

4. После успешного переноса задачи в Роадмап, **немедленно** перемести исходный файл из `_TODO_/` в `_TODO_/done/`.
5. Если обработанный инбокс сгенерировал задачу, которая должна выполняться прямо сейчас (имеет высший приоритет или продолжает текущий этап), обнови раздел `Задача из ROADMAP` в файле `_STATE_.md`.


Шаг 3: Планирование (Микро-декомпозиция)

1. Возьми активную задачу (из `_STATE_.md` или верхнюю приоритетную задачу из `ROADMAP.md`).
2. Поставь этой задаче в `ROADMAP.md` статус `⏳ выполняется` (используй точечный `patch/diff`, не затирай соседние строки).
3. Разбей задачу на атомарные микро-шаги в `_STATE_.md`, строго следуя **Правилам микро-декомпозиции**:

   - **Правило "Один шаг — одна сущность":** Запрещено создавать шаги, содержащие союзы "И", "МАППИНГ + UI", "ЛОГИКА И ТЕСТЫ". Один шаг должен решать ровно одну изолированную подзадачу (например: только добавить структуру в `settings.rs`, только пробросить колбэк в `ui_manager.rs`, только написать один юнит-тест).
   - **Правило локализации:** Один микро-шаг должен затрагивать **не более 1–2 файлов** кода. Если для шага требуется изменить 4 файла — разбей этот шаг на два или три независимых подшага.
   - **Правило явного Definition of Done (DoD):** Каждый шаг в итерационном трекере должен содержать четкий технический критерий проверки (например: `[ ] Шаг 1: Добавить поле в Settings. Проверка: cargo check без ошибок`, `[ ] Шаг 2: Написать тест test_bit_perfect. Проверка: cargo test test_bit_perfect зелёный`).
   - **Правило иерархии:** `ROADMAP.md` содержит атомарные требования/подзадачи (ID `V5.1-X.Y[.Z]`, см. Регламент §3). `_STATE_.md` — ещё более мелкая декомпозиция ТЕКУЩЕЙ задачи на микро-шаги (один шаг = одна сущность, ≤1–2 файла). В `_STATE_.md` держится только ОДНА активная задача — из строки `⏳ выполняется` в `ROADMAP.md`.

4. Полностью сформируй и запиши обновленный `_STATE_.md` по утвержденному шаблону из Шага 0. 
5. Строго заполни поле `Вайтлист файлов в работе (Изменяемые файлы)`. Внеси туда только те файлы, которые физически необходимо мутировать для выполнения составленного плана. 
6. Выведи сформированный микро-план и вайтлист файлов в чат пользователю и напиши: 
   *"План микро-сессии сформирован. Вайтлист файлов заблокирован. Готов приступить к Шагу 1."*


Шаг 4: Цикл Выполнения (Итерационный кодинг)

Выполняй каждый микро-шаг плана строго по одной итерации за раз, следуя алгоритму:

1. **Проверка Вайтлиста:** Перед изменением любого файла убедись, что он внесен в `Вайтлист файлов в работе` в `_STATE_.md`. 
   - *СТРОГОЕ ТАБУ:* Если файла нет в вайтлисте, тебе ЗАПРЕЩЕНО использовать к нему инструменты записи (`write/patch`). Если файл критически необходим — сначала остановись и запроси у пользователя разрешение на расширение вайтлиста.

2. **Внесение изменений:** Напиши или измени код для текущего `current_step`.
   - *Запрет на заглушки:* Категорически запрещено писать `// TODO`, `// ... остальной код`,  удалять существующие функции, если об этом не просили прямо,а также оставлять пустые callback/property в файлах `ui/*.slint` или скрывать нереализованный UI через прозрачность. Если меняешь файл (Rust или Slint) — пиши код полностью от начала до конца, сохраняя окружающий контекст и комментарии.


3. **Верификация (Сборка и Тесты):**
   - `cargo check` — выполняй **после каждого микро-шага** (проверка компиляции и типов, дёшево по времени и токенам).
   - `cargo clippy` и `cargo test` — полный прогон **не обязателен на каждом шаге**. Гоняй их раз в 2-3 микро-шага и обязательно перед финальным коммитом задачи (Шаг 5). Для одиночного шага, где это уместно, можно ограничиться `cargo test <фильтр>` по конкретному затронутому модулю вместо полного набора.
   - `cargo clippy` линтит весь крейт, а не отдельные файлы — сравнивай вывод не построчно целиком, а фильтруй по путям файлов из текущего вайтлиста (`cargo clippy --message-format=json | ...` или простой grep по пути в тексте предупреждения). Не держи в контексте полный лог дважды («до» и «после») — сверяй только строки, относящиеся к изменённым файлам.

4. **Обработка РЕЗУЛЬТАТОВ верификации:**

   - **СЦЕНАРИЙ А: УСПЕХ (Все проверки зеленые):**
     - Сбрось `Счетчик безуспешных компиляций` в `_STATE_.md` на `0/3`.
     - Отмечай шаг выполненным `[x]` в итерационном трекере.
     - Запиши в `_STATE_.md` новый `current_step` и `следующий ход`.
     - Сделай git-коммит. *ПРАВИЛО БЕЗОПАСНОСТИ:* Коммит должен включать измененный код и файл `_STATE_.md` одновременно. Снабжай коммит понятным техническим сообщением (например, `feat(audio): add exclusive ALSA mode check`).
     - Переходи к следующему шагу (возврат на пункт 1).

   - **СЦЕНАРИЙ Б: ПРОВАЛ (Ошибки компиляции или падение тестов):**
     - Считай значение `Счетчик безуспешных компиляций` из `_STATE_.md` и увеличий его на 1.
     - **Если счетчик достиг 3/3 (Критический тупик):** Перед откатом зафиксируй в `_STATE_.md` (раздел "Опробованные и неудачные подходы") тезисный список всех попыток текущего шага — не сам код, а кратко суть подхода и почему он не сработал (например: «1) прямой `as` каст — компилятор ругался на переполнение; 2) `try_into` без `.expect()` — не собиралось из-за `?` в non-Result fn»). Это нужно, чтобы при возврате пользователя следующая попытка не повторяла уже отработанные тупиковые пути.
       БЕЗУСЛОВНЫЙ СТОП. Выполни отмену изменений текущего шага (`git checkout -- [файлы из вайтлиста]`), чтобы вернуть репозиторий в последнее рабочее состояние коммита (список опробованных подходов в `_STATE_.md` при этом сохраняется, т.к. он не в вайтлисте кода). Выведи в чат лог ошибки и сообщение: 
       *"🚨 Достигнут лимит ошибок (3/3). Я зашел в тупик, не могу исправить сборку и выполнил откат изменений этого шага к последнему коммиту. Опробованные подходы зафиксированы в _STATE_.md. Передаю управление человеку."* Прекрати работу.
     - **Если счетчик меньше 3:** Запиши новое значение счетчика (например, `1/3`) и описание ошибки в `_STATE_.md`. Попробуй ОДИН альтернативный вариант решения (не пиши тот же код заново!). Повтори пункт 3.
5. **Выход из дедлупа (Инструкция после отката):**
   - Если пользователь вмешивается после того, как ты достиг лимита 3/3 и откатил код, ты обязан полностью стереть из своей памяти старое (неудачное) техническое решение текущего шага.
   - Внимательно прочитай текстовое указание пользователя. 
   - Сбрось счетчик ошибок на 0/3.
   - Перепиши ТЕКСТ текущего микро-шага в `_STATE_.md`, отразив в нем новую траекторию, предложенную пользователем, и только после этого делай вторую попытку написания кода.


Шаг 5: Завершение (Закрытие сессии)

Если все микро-шаги в `_STATE_.md` успешно выполнены `[x]`, а финальная верификация (`cargo test` и `cargo clippy`) полностью зелёная и не содержит новых предупреждений:

1. **Актуализация бэклога проекта:**
   - Обнови статус выполненной задачи в `ROADMAP.md` (используй точечный `patch/diff`).
   - Если завершен крупный этап, перенеси его текстовые подробности и историю реализации в файл `_TODO_/history/archive_vX.Y.md`, оставляя в основном `ROADMAP.md` только лаконичную строку глобального статуса. Очищай контекст для следующих сессий.
   - Строго укажи формат: `✅ сделано в <short_commit_hash> (ветка <branch_name>)`. Не затирай и не ломай структуру соседних этапов.

2. **Консервация файла состояния сессии:**
   - Полностью очисти и переведи `_STATE_.md` в финальный статус `done`.
   - Внутри файла оставь только три строки:
     ```markdown
     # Состояние сессии
     - **Текущая задача:** Нет (все шаги завершены)
     - **Состояние:** done
     ```

3. **Финальный аудит изменений:**
   - Выполни команду `git diff --stat` относительно начала сессии.
   - Выведи этот отчет в чат пользователю. Это твоя метрика проделанной работы.

4. **Формирование итогового отчета:**
   - Напиши краткое, технически точное резюме в чат:
     - Что было сделано (список измененных модулей и ключевых структур).
     - Какие тесты подтверждают работоспособность.
     - Какой следующий логический ход (следующая задача по приоритету из `ROADMAP.md`).
   - Заверши сессию фразой: *"Задача полностью завершена, репозиторий чист, файлы состояния законсервированы. Готов к новой задаче."*


## Интерактивный режим (Задачи напрямую из чата)

Если пользователь ставит задачу или просит внести правки напрямую в чате (минуя каталог `_TODO_/`), эта задача считается полноценной и приоритетной. ТЕБЕ ЗАПРЕЩЕНО писать код до тех пор, пока ты не выполнишь процедуру логирования:

1. **Регистрация в ROADMAP.md:**
   - Определи, к какому этапу относится задача. Если это мелкая правка или баг, добавь её в текущий активный этап или в раздел `Future-блок / Баг-раунд`.
   - Используй инструмент `patch/diff` для точечной вставки новой строки. Назначь приоритет (`🔴/🟡/🟢`) по правилам Шага 2.
   - **Классификация пути задачи:**
     - **Точечный баг/правка** (≤1–2 файлов, без новой функциональности) → напрямую в `ROADMAP.md` и далее по пунктам 2–4 этого режима.
     - **Новая фича / крупная доработка** (несколько модулей или новое поведение) → сначала полный воркфлоу Регламента §2 (уточняющие вопросы → финальный текст ТЗ → явное разрешение → сохранение спецификации в `docs/`), затем добавление задачи в `ROADMAP.md` со ссылкой на созданный документ. Только после этого продолжить с пункта 2.

2. **Инициализация сессии в _STATE_.md:**
   - Полностью перезапиши `_STATE_.md` по машинно-читаемому шаблону из Шага 0.
   - Проведи микро-декомпозицию этой задачи на понятные шаги с критериями Definition of Done (DoD) по правилам Шага 3.
   - Сформируй жесткий вайтлист изменяемых файлов.

3. **Синхронизация с пользователем:**
   - Выведи в чат сообщение: *"Задача из чата зарегистрирована в ROADMAP.md и декомпозирована в _STATE_.md. Перехожу к Шагу 4 (Цикл выполнения)."*

4. **Выполнение и закрытие:**
   - Проведи задачу через стандартный **Шаг 4 (Цикл выполнения)** и **Шаг 5 (Завершение)**. Никаких исключений или сокращений воркфлоу для задач из чата не допускается.


## 📐 РЕГЛАМЕНТ УПРАВЛЕНИЯ ТРЕБОВАНИЯМИ И ЖИВОГО ВЕРСИОНИРОВАНИЯ ДОКУМЕНТАЦИИ

### 1. Единое пространство спецификаций (`docs/`)
Все технические спецификации, ТЗ и описания фич хранятся строго в директории `docs/` в виде Markdown-файлов. Отдельные файлы для мелких задач или RFC не создаются — всё консолидируется в рамках логических модулей (например, `docs/spec_visualizer_v5.1.md`).

### 2. Живой интерактивный воркфлоу для новых задач
Если пользователь добавляет новую задачу в `_TODO_` в упрощенном виде или ставит её в интерактивном режиме:
1. Агент обязан проанализировать её и задать пользователю уточняющие инженерные вопросы.
2. Когда все детали ясны, агент выводит финальный структурированный текст ТЗ для оценки пользователем. Черновик ТЗ существует только в чате — файлы в `_DRAFTS_/` не создаются и не читаются.
3. Агент запрашивает явное разрешение на изменение папки `docs/` и сохраняет туда новую спецификацию (или дополняет существующую).
4. Только после этого задача заносится в бэклог `ROADMAP.md` со ссылкой на созданный документ.

### 3. Сквозная трассируемость (Traceability Matrix)

- Каждая задача в таблице бэклога `ROADMAP.md` ОБЯЗАНА иметь прямую ссылку на параграф или якорь в файле документации.
- **Соглашение нумерации задач (ID):** `<Префикс-спеки>-<параграф ТЗ>[.номер подзадачи]`. Префикс уникально объявляется в шапке документа-спеки в `docs/` (`spec_visualizer_v5.1.md` → `V5.1`; будущие `spec_audio_v6.8.md` → `A6.8`, `spec_ui_v7.0.md` → `U7.0`). Новый префикс не должен конфликтовать с уже зарегистрированными (реестр — в шапке `ROADMAP.md`). Подзадачи добавляются суффиксами (`.1/.2/...`).
- **Формат ссылки относительно корня проекта:** `[ТЗ §X.Y](docs/spec_visualizer_v5.1.md#<якорь>)`.
- **Экономия контекста при чтении:** по ссылке трассируемости читай только параграф/секцию по указанному якорю, а не весь файл `docs/spec_*.md` целиком. Весь документ читается только если задача явно требует понимания смежных секций.
- **Стиль якоря (GitHub text):** образуется из текста заголовка ТЗ — нижний регистр, знаки (точки, двоеточия, слеши) удаляются, пробелы заменяются на `-`. Пример: `### 7.2. Влияние на громкость` → `#72-влияние-на-громкость`.
- **Иерархия:** `ROADMAP.md` = атомарные требования/подзадачи по ID спеки; `_STATE_.md` = микро-шаги текущей задачи (см. Шаг 3, Правило иерархии).

### 4. Стандартизация Архивов (Технический паспорт решения)
При переносе выполненных этапов в файлы `_TODO_/history/archive_vX.Y.md` запрещено слепо стирать текст. Модель обязана составить краткий технический паспорт решения: коммит, дата, примененные формулы, созданные структуры данных, размеры буферов/FFT и зашитые Real-Time константы для сохранения контекста.


## Стиль кода, SOLID и инженерные правила

Ты обязан писать код, соответствующий уровню **Staff System Engineer**, строго соблюдая архитектурную парадигму SOLID и специфику высокоточного аудио-движка:

### 1. Архитектурные принципы SOLID в Rust-реализации

* **S (Single Responsibility / Единственная ответственность):** 
  Каждый модуль в `src/audio/` и каждый менеджер в `src/app/` решает только одну задачу. Логика декодирования (Symphonia), логика трансформации потока (Ресемплинг/Дизеринг) и логика вывода (Cpal) должны быть строго изолированы друг от друга. Потребители получают данные только через направленный `AppEvent-feed`.
* **O/D (Open-Closed & Dependency Inversion / Открытость к расширению и Инверсия зависимостей):** 
  Прямая зависимость от конкретных системных бэкендов (ALSA, WASAPI, ASIO) запрещена. Все взаимодействия с оборудованием и алгоритмами ресемплинга осуществляются через трейты (`AudioHost`, `AudioSource`). Поддержка нового режима вывода (например, WASAPI Exclusive) или нового алгоритма апсемплинга должна происходить путем создания новой структуры, реализующей существующий трейт, без изменения логики ядра `Player`.
* **I (Interface Segregation / Разделение интерфейсов):** 
  Трейты должны быть точечными. Поведение разделено на обязательный стриминг кадров и опциональные возможности (например, методы `seek()` или `duration()` в `AudioSource` имеют безопасные дефолтные реализации, избавляя DSD-декодеры от избыточного кода).

### 2. Безопасность памяти, типов и защита звукового тракта (No Clipping)

* **Абсолютное табу на `unwrap()` и скрытые паники:** 
  В коде аудио-движка вызовы `.unwrap()` **СТРОГО ЗАПРЕЩЕНЫ**. Используй `?`, `let-else` или консервативный `expect()`. Любая паника в монопольном режиме намертво вешает аудио-подсистему ОС.
* **Запрет на небезопасное приведение типов (Предотвращение цифровых щелчков):** 
  При масштабировании сэмплов или изменении разрядности (например, при расчете громкости или подготовке кадров) запрещено использовать оператор `as`, способный вызвать бесшумное переполнение и искажение сигнала. Используй строго `.clamp()` и безопасные методы преобразования с контролем границ (`try_into()`, `from_sample()`).
* **Бережная работа с Borrow Checker:** 
  Запрещено использовать `.clone()` на тяжелые объекты (`Track`, `Vec`, `Settings`) ради обхода ограничений времени жизни. Передавай данные по ссылкам `&`, управляй разделяемыми ресурсами через атомики и связку `Arc<Mutex>` / `Arc<RwLock>` только на границах потоков.

### 3. Потоковый аудио-DSP код реального времени (Hard Real-Time Constraints)

* **0 аллокаций в горячем цикле:** 
  Аудио-колбэки `cpal` и внутренние циклы ресемплеров/анализаторов должны работать с нулевым выделением памяти в куче (`Vec`, `Box`, `String`). Все буферы (включая DoP-контейнеры и FFT-окна) должны быть аллоцированы заранее при инициализации структур.
* **Запрет на блокировки и логирование в аудио-потоке:** 
  Внутри колбэков вывода звука запрещено использовать любые длительные блокирующие примитивы (`Mutex` не должен удерживаться), а также любые макросы логирования (`println!`, `eprintln!`, `tracing::*`). Сбой тайминга в аудио-потоке из-за вывода в консоль — это критическая ошибка (Drop-out/Заикание).

### 4. Инкапсуляция "Ромба" и аудиофильская синхронизация Slint UI

* **Инкапсуляция состояния:** 
  Все мутируемые поля состояния приложения инкапсулированы внутри `src/app/mod.rs`. Подмодули расширяют логику через `impl MusicApp` и общаются через видимость `pub(super)`.
* **Синхронизация Bit-perfect режима:** 
  Если в настройках активирован режим `bit_perfect`, код синхронизации интерфейса в `ui_manager.rs` обязан принудительно блокировать (`set_enabled(false)`) программные ползунки громкости, баланса и эквалайзера в Slint, переводя тракт в режим Direct Output (100% громкости на ЦАП без пересчета сэмплов). 
* **Режим Mixed:** 
  В обычном режиме (Mixed) громкость считается в точности 32/64-bit float с обязательным применением треугольного дизеринга (TPDF) перед выводом. 
  *КРИТИЧЕСКИЙ ГАЙДЛАЙН ДЛЯ DSP:* Запрещено использовать блокирующий или медленный `rand::thread_rng()` внутри аудио-колбэка для генерации TPDF. Шум должен вычисляться через сверхбыстрый безалокационный генератор (например, простой линейный конгруэнтный генератор LCG / PRNG, сдвиговый регистр LFSR или предвычисленный массив/LUT-таблицу), чтобы гарантировать соблюдение Hard Real-Time ограничений.


---

## ТЕХНИЧЕСКИЙ КОНТЕКСТ ПРОЕКТА

Музыкальный плеер на Rust + Slint. Классическое ядро-библиотека (`music_player_rs`) + бинарник (`music-player-rs`) на Slint-интерфейсе, системный трей через StatusNotifier (ksni).

### Команды верификации

```sh
cargo build          # dev-сборка
cargo build --release # релиз (opt-level=3, lto="fat", target-cpu=native)
cargo run            # запустить плеер
cargo test           # юнит-тесты: 101 lib (cover, settings, playlist, playlist_layout, audio/*) + 10 bin (app/mod.rs, app/viz_settings_manager.rs) (полный прогон раз в 2-3 шага и обязательно перед финальным коммитом задачи; на промежуточных шагах допустим `cargo test <фильтр>` по затронутому модулю)
cargo test <name>    # один тест по фильтру
cargo test audio::   # запуск только тестов аудио-подсистемы
cargo clippy         # линт (0 новых варнингов в измененных файлах)
python tools/acceptance.py status  # кросс-ОС приёмка спеки: отдельный этап при закрытии спеки, НЕ после каждой задачи (регламент — docstring скрипта)
```

Сборка **требует** системные пакеты: `g++` и `libstdc++-dev` (Skia GPU-рендер Slint).
При `cargo build` компиляция `.slint` печатает warning'и (padding/width-height) — это штатно, не ошибки.

### Архитектурная карта исходников

```
build.rs             # компилирует ui/app.slint в Rust (build-dependency: slint-build)
ui/                  # Slint UI (.slint)
  app.slint          # AppWindow — собирает всё вместе, экспортирует колбэки и свойства
  top_panel.slint    # верхняя панель (обложка, трек, управление)
  playlist.slint     # таблица плейлиста
  tableview_fork.slint   # форк Slint TableView (сортировка/клики)
  fork_components.slint  # вспомогательные компоненты для таблицы (depends)
  status.slint       # строка состояния
  settings.slint     # диалог настроек (вкл. вкладку Visualization)
  visualizer.slint   # виджеты визуализации (спектр/осциллограмма/спектрограмма)
  theme.slint        # палитра цветов (тёмная/светлая)
  icons/             # SVG-иконки
src/
  lib.rs             # корневая библиотека: audio, cover, meta, playlist, playlist_layout, settings, tray
  main.rs            # бинарник — создаёт AppWindow, MusicApp, таймер tick (100 мс), run_event_loop_until_quit
  app/               # MusicApp разбит на модули (God Object → менеджеры)
    mod.rs           # struct, new/init/tick, bind_callbacks, трей, helpers, тесты
    events.rs        # направленный event-feed (AppEvent) между менеджерами
    ui_manager.rs        # UI-синхронизация, колонки плейлиста, темы (impl MusicApp)
    playback_manager.rs  # воспроизведение/транспорт, shuffle, обложки (impl MusicApp)
    playlist_manager.rs  # трек-лист, сканирование папок, сортировка (impl MusicApp)
    visualizer_manager.rs # live-спектр: воркер анализа, tap-проводка, бары в окно (impl MusicApp)
    fulltrack_manager.rs  # полнотрековые визуализации: осциллограмма/спектрограмма (impl MusicApp)
    viz_settings_manager.rs # настройки визуализации в диалоге (вкладка §9), тесты
  cover.rs           # обложки альбомов: папка/embedded/internet, фоновый воркер, кэш на диск
  meta.rs            # метаданные треков (symphonia probe)
  playlist.rs        # загрузка/сохранение M3U, сканирование папок, Track, сортировка
  playlist_layout.rs # итеративный распределитель ширин колонок (таблица заполняет контейнер точно)
  settings.rs        # настройки (toml), SettingsStore, ColumnId, RepeatMode, AppConfig
  tray.rs            # трей (ksni): PlayerTray, TrayCmd, TrayState, start()
  audio/             # аудио-подсистема
    mod.rs
    player.rs        # Player (управление воспроизведением)
    decoder.rs       # декодер (symphonia)
    dsd.rs           # DSD-декодер (DSF/DFF, CIC)
    output.rs        # устройство вывода (cpal), выбор устройства, bit-perfect, ресемплер
    analyzer.rs      # live-анализатор спектра (SpectrumEngine, «лестничный» DSP без аудио)
    spectrogram.rs   # полнотрековая спектрограмма (БПФ, частота × время)
    fulltrack.rs     # полнотрековая осциллограмма (envelope + RGBA-рендер + PNG-кэш)
    palettes.rs      # палитры (magma/inferno/plasma/viridis, 256-уровневые LUT)
    visualizer.rs    # настройки визуализации и общие модели (3 режима: спектр/осциллограмма/спектрограмма)
```

- `app/*` — «Ромб-декомпозиция»: методы `MusicApp` разнесены по `impl`-блокам в подмодулях. Все поля остаются в `mod.rs`, подмодули вызывают методы друг друга через `pub(super)`.

### Как связывается Slint

- `build.rs` компилирует `ui/app.slint` → генерирует структуру `AppWindow` в Rust (`slint::include_modules!()` в `src/app/mod.rs`).
- `AppWindow` содержит `in-property`, `callback` и т.д.; Rust подписывается через `ui.on_<callback>(...)` и читает/пишет через `ui.set_<prop>(...)` / `ui.get_<prop>()`.
- Slint-файлы импортируют друг друга через `import { Name } from "file.slint";`. `app.slint` — точка входа, остальные — компоненты.
- Стиль UI задан в `build.rs` (`fluent`).

### Единый экземпляр настроек (SettingsStore)

- `MusicApp` — единственный мутирующий владелец `SettingsStore`; читает с диска один раз при старте (`SettingsStore::load()`).
- Остальные модули (cover, output) получают `Settings` явно по ссылке через параметры (`CoverConfig::from_settings(&Settings)`); глобального снапшота конфига нет.

## Features

Собственных Cargo features нет (в Cargo.toml `[features]` отсутствует).

Зависимости подключают свои features:
- `slint` 1.17 — `std, compat-1-2, backend-winit-x11, renderer-winit-skia` (X11-бэкенд, Skia GPU-рендер; требует g++ и libstdc++-dev в системе); build-dependency: `slint-build` 1.17.
- `symphonia` 0.6 — кодеки: `flac, wav, aiff, pcm, mp3, ogg, aac, alac, isomp4, vorbis, adpcm`.
- `tokio` 1.53 — `rt, macros, sync` (только для `mpsc` в tray).
- Аудио/визуализация: `cpal` 0.18 (вывод), `rustfft` 6.4 (БПФ), `rtrb` 0.4 (кольцевой буфер), `clru` 0.6 (LRU-кэш изображений).
- Данные/интеграции: `rfd` 0.17 (диалоги файлов/папок), `ureq` 2 + `tls` (HTTP для обложек), `serde`/`serde_json`/`toml` (сериализация), `dirs` 6, `walkdir` 2 (сканирование библиотеки), `rand` 0.8 (shuffle), `image` 0.25 + `png` (рендер обложек/кэш).
- Трей: `ksni` 0.3.6 (StatusNotifier).

## Запуск с разными конфигурациями

- Обычный запуск: `cargo run`.
- Трей требует StatusNotifier-хост (KDE/GNOME AppIndicator). Если хоста нет, трей молча не появляется — приложение продолжает работать.
- Эмуляции бэкенда/winit в коде нет. Для headless-тестирования Slint предоставляет тестовый бэкенд, но в проекте он не настроен.

## Тесты

- Модульные тесты: `src/cover.rs` (`percent_encode`, `sniff_detects_jpeg_and_png`, `folder_cover_respects_name_order`, `write_cover_returns_existing_cache`, `cover_priority_keys_roundtrip`, кэш/hash), `src/settings.rs` (колонки, cover-приоритеты, info-labels, аудио/DSD, toml-раундтрипы), `src/playlist.rs`, `src/playlist_layout.rs` (заполнение контейнера/минимальные ширины), `src/audio/dsd.rs`, `src/audio/decoder.rs`, `src/audio/player.rs`, `src/audio/output.rs` (выбор устройства/ресемплер), `src/audio/analyzer.rs`, `src/audio/spectrogram.rs`, `src/audio/fulltrack.rs`, `src/audio/palettes.rs`, `src/audio/visualizer.rs`; bin-тесты — в `src/app/mod.rs` и `src/app/viz_settings_manager.rs`.
- Каталогов `tests/` (интеграционных) нет.
- `cargo test`: 101 lib + 10 bin.
