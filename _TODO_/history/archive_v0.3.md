# Архив реализации: ЭТАП 6 (V5.1) + сессия R1–R3 + релиз v0.3

> Файл создан при чистке ROADMAP.md 2026-09-15.
> Все коммиты влиты в `main`. Релиз `v0.3` собран в `releases/v0.3/`.
> Подробная техническая история визуализатора (Этапы 1–5 и каркас/анализатор/
> ресемплеры/осциллограмма/спектрограмма ЭТАПА 6) — в `archive_v0.2.md`.

---

## 1. Релиз v0.3 и оперативные задачи сессии (R1–R3)

### 1.1. R1 — сборка релиза v0.3

- **Коммит:** `bcc304a` (ветка `main`).
- `Cargo.toml`/`Cargo.lock`: version `0.2.0` → `0.3.0`.
- `cargo build --release` — без предупреждений (профиль: opt-level=3, lto=fat,
  target-cpu=native). Бинарь `target/release/music-player-rs` (30.1 MB) →
  `releases/v0.3/music-player-rs` (каталог `releases/` в .gitignore, артефакты
  не трекаются — конвенция v0.1/v0.2).
- Заодно найден и восстановлен потерянный вызов `a.set_bp_notice()` в
  settings-save (см. п.1.4).

### 1.2. R2 — закрытие спеки V5.1

- **Коммиты:** `cba2702`, `6864db6` (ветка `main`).
- `docs/spec_visualizer_v5.1.md` объявлена выполненной (Этапы 1–5 +
  визуализация уже в `archive_v0.2.md`).
- FUTURE-БЛОК удалён из ROADMAP (идеи — вне бэклога агента, у пользователя).

### 1.3. R3 — спека тем переведена в docs/

- **Коммиты:** `bd4b222` (перенос `_TODO_/spec_theme_v1.0.md` → `_TODO_/done/`),
  `850e7a7`, `8697ae1` (ветка `main`).
- Вдумчивый разбор `_TODO_/spec_theme_v1.0.md` против кода: подтверждены ровно
  22 цвета `ColorsData` (сверено с `ui/theme.slint` и `apply_theme()`), механизм
  draft/save, иконки через `root.theme`, crate-граница lib/bin.
- 14 уточняющих вопросов → финальное ТЗ с правками пользователя →
  согласовано → **`docs/spec_theme_v1.0.md` (префикс `T1.0`)**, зарегистрирован
  в шапке ROADMAP.md.
- Открыт **ЭТАП 7** (`T1.0`, бэклог).

### 1.4. Fix-хэтро: потерянный вызов tray-notice при BP

- **Коммит:** `bd4b222`.
- Симптом: release-сборка падала `dead_code` — `set_bp_notice()` не вызывался
  нигде (вызов потерялся ещё при имплементации V5.1-8.7 в `15f7ad6`, как ранее
  терялось объявление поля).
- Фикс: `a.set_bp_notice()` в блоке `if bp { ... }` settings-save
  (`src/app/mod.rs:940`).

---

## 2. ЭТАП 6 — Аудиофильский звуковой тракт и полировка (V5.1)

> Статус: спека V5.1 считается выполненной. Остаются два открытых пункта:
> `V5.1-8.6.2` (ручная верификация DoP на железе) и `V5.1-11`
> (сквозное документирование бэклога). Информация ниже — трассировочный
> паспорт подзадач (коммит × структура × константы).

### 2.1. Bit-perfect / Direct Output (§7)

| ID | Коммит | Технические детали |
| --- | --- | --- |
| V5.1-7 | `20080d8` | BP (Direct Output, unity gain, снятие mute), TPDF-дизеринг, DSD native заглушка / DoP MVP. TPDF — LCG без аллокаций в аудио-колбэке. |
| V5.1-7.2 | `e114113` → ↩️ снято в `V5.1-8.7` | Подпись «Управляется внешним предусилителем / ОС» удалена дизайн-решением. |
| V5.1-7.5 | `aa86c98` | Индикация BP в статус-баре. |
| V5.1-7.4 | `bb4bed0`, `a8f9c81`, `82421ad` | DSD→PCM несовместимо с BP: `Settings::dsd_pcm_breaks_bit_perfect`, переключение сценариев ComboBox DSD-mode, warning в settings + индикатор «Не bit-perfect (DSD→PCM)». |
| V5.1-8.7 | `15f7ad6`, `eeff631`, `bd4b222` | Дизайн-delta: BP-переключатель только в окне настроек; трай-тултип `TrayState.notice` (BP-notice, `BP_NOTICE_TEXT`) при транзите off→on, дедлайн `Instant + 5 c`; игнор регулировки громкости в BP (tray wheel, UI slider `enabled: !bit-perfect`, `volume-changed`). |

### 2.2. DSD → PCM / Ресемплинг (§8)

| ID | Коммит | Технические детали |
| --- | --- | --- |
| V5.1-8.5 | `7457731`, `de797c0`, `d8aab8d` | `target_sample_rate = auto` → ближайшая частота ЦАП; чистая `nearest_rate(ranges,channels,requested)`; тесты match/fallback/44.1k-48k семейства, tie-break. |
| V5.1-8.6 | `d0dd533` | UI: чекбокс BP (§7.5), live+save apply. **8.6.2 — ручная верификация DoP-бит-потока (0x05/0xFA) на железe, ⬜ ждёт устройства.** |

### 2.3. Память, кэширование и персистентность (§10)

| ID | Коммит | Технические детали |
| --- | --- | --- |
| V5.1-10.5 | `982250d`, `f42c62c`, `064d947`, `1025337` | Вкладка «Кэш»: `cache_sizes() -> (ram_bytes, disk_bytes)`; блок UI в settings.slint; проброс open-dialog → tick sync. |
| V5.1-10.5.2 | `4bb51df` | Кнопки очистки (визуализация/обложки/всё). |
| V5.1-10.5.3 | `6673e15` | Рекурсивная очистка hash-поддиректорий обложек, `cover_cache_size()`. |
| V5.1-10.4.1 | `c04fe1a`, `e6cdef7`, `ab42d4b`, `5c78194` | RAM-лимит `viz_max_ram_mb` (=64 MB): `max_entries = mb / avg_rgba_mb` → `NonZeroUsize`; горячий `CLruCache::resize`. |
| V5.1-10.4.2 | `e8ebcae`, `5114567`, `29a84d7` | Disk-лимит `cache.max_size_mb` + LRU по mtime; вытеснение после `save_png`. |
| V5.1-10.4.3 | `52e0adf`, `6e138df`, `ff95ce0` | TTL-инвалидация по mtime; ключ кэша = `hash(path+mtime+size+mode+channels+params+dsd)`; sidecar mtime vs source. |

### 2.4. Взаимодействие с плеером (§13)

| ID | Коммит | Технические детали |
| --- | --- | --- |
| V5.1-13.3 | `a53be30` | Seek по клику/драгу: TouchArea X→fraction, callback `viz-seek`, переиспользован `seek-commit` (app.slint→Rust), драг не инициирует fulltrack-билд. |

### 2.5. UI/UX полировка (§15) и дизайн-дельты

| ID | Коммит | Технические детали |
| --- | --- | --- |
| V5.1-15.1.1 | `bf5d7a6` | Миграция Material → Fluent (fix прижатия заголовков колонок). |
| V5.1-15.2 | `bf5d7a6` | Fix проглатывания первого клика диалога (FocusScope+Escape+backdrop scroll). |
| V5.1-15.3 | `bf5d7a6` | Кнопки не выпирают за диалог. |
| V5.1-15.4 | `bf5d7a6` | Редизайн диалога настроек. |

---

## 3. Что осталось / следующий шаг

- `V5.1-8.6.2` — ручная DoP-верификация на реальном DSD-ЦАП (ждёт железа).
- `V5.1-11` — бэклог: сквозное документирование (§11 RT-бюджеты, реестры).
- **ЭТАП 7** — реализация системы тем из TOML по спеке `docs/spec_theme_v1.0.md`
  (задача `T1.0`, бэклог; декомпозиция в `_STATE_.md` при старте сессии).